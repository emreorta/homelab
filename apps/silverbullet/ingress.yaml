apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: silverbullet
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`notes.${DOMAIN}`)
      kind: Rule
      middlewares:
        - name: authelia-auth
          namespace: traefik
      services:
        - name: silverbullet
          port: 80
          namespace: silverbullet
  tls: {}
