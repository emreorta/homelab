apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: dozzle
  namespace: traefik
spec:
  entryPoints:
    - frp
  routes:
    - match: Host(`logs.${DOMAIN}`)
      kind: Rule
      middlewares:
        - name: frp-headers
          namespace: traefik
        - name: authelia-auth
          namespace: traefik
      services:
        - name: dozzle
          port: 80
          namespace: dozzle
