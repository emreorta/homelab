apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: authelia
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`auth.${DOMAIN}`)
      kind: Rule
      services:
        - name: authelia
          port: 80
          namespace: authelia
  tls: {}
